{
  "name": "adaptisearch",
  "version": "1.0.0",
  "description": "Generic adaptive search — trie, inverted index, red-black tree, categorical hash",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "demo": "vite",
    "demo:build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.11",
    "vitest": "^4.1.5"
  }
}
