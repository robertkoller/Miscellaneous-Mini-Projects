// Public API — re-exports SearchEngine and the types needed to use it.

export { SearchEngine } from './search-engine';
export type { SearchQuery, SearchResult, Filter, RangeFilter, ExactFilter } from './types';
